using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fuente : MonoBehaviour
{
    //    public int vidaIncremento = 10; 


    //    // Start is called before the first frame update
    //    void Start()
    //    {

    //    }

    //    // Update is called once per frame
    //    void Update()
    //    {


    //    }
    //    private void OnTriggerEnter2D(Collider2D other)
    //    {
    //        if (other.CompareTag("Player")) // Ajusta la etiqueta según la que uses para el jugador
    //        {
    //            VidaJugador vidaJugador = other.GetComponent<VidaJugador>(); // Asegúrate de tener un script VidaJugador en el objeto del jugador

    //            if (vidaJugador != null)
    //            {
    //                vidaJugador.AumentarVida(vidaIncremento);
    //                // Otra lógica aquí si es necesario (por ejemplo, reproducir un sonido, desactivar la fuente, etc.)
    //                Destroy(gameObject); // Puedes destruir la fuente después de ser recogida
    //            }
    //        }
    //    }
}
 